<% if !@projets_restants.empty? %>
  <%= form_for(:todoitem, :url => "/tutelle_subscriptions/create?tutelle_id="+@tutelle.id.to_s,:remote => true, :html => {:class => 'inline',:method => :post}) do |f| %>
  	<label for="tutelle_subscription_projet_id">Equipes disponibles</label>
  	<%= select "tutelle_subscription", "projet_id", @projets_restants.collect {|p| [ p.nom, p.id ] } %>		
    	<%= submit_tag "Ajouter l'association" %>
  <% end %>
<% end %>

